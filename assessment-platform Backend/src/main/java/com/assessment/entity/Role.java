package com.assessment.entity;

/**
 * Represents the role of a user in the system.
 * Maps to the ENUM('STUDENT','TEACHER') column in the users table.
 */
public enum Role {
    STUDENT,
    TEACHER
}
